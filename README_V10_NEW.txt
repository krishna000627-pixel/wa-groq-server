ARIA V10 NEW — FROM-SCRATCH WEB UI

This package is a fresh build, not a renamed copy of the previous ZIP.

Included:
- dashboard.html — responsive Aria dashboard
- assets/app_icon.png — embedded generated Aria artwork
- assets/banner.png — embedded generated home banner artwork
- assets/welcome_card.png — embedded welcome card artwork
- assets/logo.png — embedded logo artwork
- manifest.webmanifest
- aria_prompt.txt — Aria behavior profile
- wa_groq_server.py — minimal local Groq-compatible server

UI rules:
- Compact reference-style typography and spacing
- Settings rows show only bold white titles
- Groq/API key is not exposed in the settings UI
- About contains Krishna Tiwari / Class 12 / PCM profile and routine
- Contexts includes a TESTUSER demo card so incoming-chat behavior can be previewed
- Image requests never cause code output unless code is explicitly requested
