ARIA V17 — CONTEXT TAP FIX

Fixes context navigation using delegated click handling and data attributes instead of inline onclick handlers. Tapping a person in Contexts now explicitly opens the dedicated context view. Recent Activity sender rows use the same navigation.

Backend/Groq SDK path is preserved from V15/V16.
