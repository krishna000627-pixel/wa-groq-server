# Aria Reply V21 — updated repo package

Repo-ready package for `wa-groq-server`.

- App source: `aria-reply-native/`
- CI: `.github/workflows/aria-reply-native.yml`
- Release signing uses GitHub Actions secrets; no release keystore is included.
